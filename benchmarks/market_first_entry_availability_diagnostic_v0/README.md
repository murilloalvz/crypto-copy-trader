# Market-First Entry Availability Diagnostic V0

Retrospective systems/execution diagnostic for the frozen Launch Burst capture. It decomposes `ENTRY_UNAVAILABLE` using acquisition-time collection status and timing metadata already preserved in `route-input-v2.json`. It does not use these fields as selector features and does not change Sniper V1 or any economic contract.
