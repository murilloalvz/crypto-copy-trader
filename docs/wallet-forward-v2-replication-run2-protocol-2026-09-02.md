# Wallet Forward v2 — Replication Run 2 Protocol (2026-09-02)

## Status

**PRE-REGISTERED / RESEARCH READ ONLY / NO PARAMETER RETUNING.**

This document freezes the protocol for one independent replication window after the quantity-aware accounting fix was validated on Wallet Forward v2 Run 1.

The purpose is sample formation and replication. It is not to rescue, confirm, or optimize the negative or positive descriptive returns observed in Run 1.

## Provenance

- prior completed run: `wallet-forward-1788360461-8a3986f9`
- quantity-aware accounting implementation: `a48c4ab3c8c19a71405323e4a22bd56bc7b73e88`
- quantity-aware result documentation: `6f89b0847b4b8dec98e9fe1c5a46b3f4751c850f`
- acquisition protocol: `wallet_forward_acquisition_v2`
- original cohort cutoff: `1788359597`

## Frozen cohort

Run 2 must use exactly the same three wallets selected before Run 1 T0:

1. `7mPtiLMhn9SsconVw8LZtrF7vL7LvLEwJv75yMLcsxTH`
2. `3tc4BVAdzjr1JpeZu6NAjLHyp4kK3iic7TexMBYGJ4Xk`
3. `2RssnB7hcrnBEx55hXMKT1E7gN27g9ecQFbbCc5Zjajq`

Do not add, remove, replace, re-rank, or refresh wallets based on activity observed after the original acquisition cutoff.

The local artifact `wallets\forward-cohort-v2.txt` generated by the frozen v2 acquisition result should be reused as-is. If that artifact is missing or its contents differ from the three addresses above, do not regenerate a new cohort from current data; stop and reconstruct only the exact frozen artifact from this preregistered list.

## Frozen runtime parameters

Run 2 must keep the same runtime settings as Run 1:

- enrollment: 4h
- follow-up: 6h
- total planned forward window: 10h
- polling interval: 10s
- RPC commitment: `confirmed`
- Jupiter quote snapshots: enabled
- quote delays: 0 / 15 / 30 / 60 / 120s after forward BUY detection
- copy notional: USDC 25
- taker: not configured
- quote mode: proxy / quote-only
- runtime family: enrollment-aware Wallet Forward v5
- mode: RESEARCH / READ ONLY

No wallet transaction is signed or broadcast by this protocol.

## Primary objectives

Run 2 is successful as a collection if the following technical conditions hold:

1. manifest reaches `COMPLETED`;
2. enrollment cutoff freezes after the planned 4h window;
3. follow-up continues for the planned additional 6h;
4. forward observations remain inside the causal run boundary;
5. every detected forward BUY receives the expected event-scoped Jupiter quote attempts for all configured delays, subject only to explicitly persisted failures;
6. RPC degradation, if any, remains auditable by run key;
7. observed signatures are audited for finality after completion;
8. quantity-aware replay is used for any descriptive economic accounting.

A zero-BUY or zero-closed-trade result is valid evidence and must not trigger an immediate replacement of wallets or parameters.

## Economic interpretation rules

Run 2 must not be judged by whether returns are positive or negative.

After completion:

- keep Run 1 and Run 2 as distinct run manifests;
- report enrolled BUY count, wallet count, token count, wallet×token clusters, concentration and censoring before aggregate return statistics;
- use quantity-aware source inventory semantics;
- preserve follow-up-only BUY exclusion from the frozen economic denominator;
- keep missing SELL quotes and ambiguous inventory explicitly censored;
- keep proxy quote evidence separate from executable transaction / landing / fill claims;
- do not promote shadow or live execution from this sample.

## Stopping rule

This preregistration authorizes exactly **one** new 10h replication window under the frozen v2 cohort and parameters.

After Run 2 completes, stop the collection sequence and audit the combined Run 1 + Run 2 evidence before deciding whether another forward protocol is justified. Do not start Run 3 automatically and do not change the protocol based solely on the sign or magnitude of Run 2 returns.

The next decision should be driven primarily by sample formation quality, dependence/concentration, censoring, quote completeness and execution realism.

## Launch command

```powershell
$env:PYTHONUTF8 = "1"
$env:DATABASE_PATH = "data\copytrader.db"

python wallet_forward_experiment.py `
  --file .\wallets\forward-cohort-v2.txt `
  --enrollment-hours 4 `
  --follow-up-hours 6 `
  --interval-seconds 10 `
  --rpc-commitment confirmed `
  --with-jupiter-quotes `
  --quote-delays-seconds 0 15 30 60 120 `
  --copy-size-usd 25 2>&1 |
  Tee-Object -FilePath .\wallet-forward-v2-run2.txt
```

Before launching, there must be no `ACTIVE` Wallet Forward Run in the same database. The orchestrator is expected to refuse overlapping runs if one exists.
